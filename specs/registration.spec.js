import { expect } from 'chai';

describe('Users - Registration', function () {
  it('should register a user with valid data', function () {
    expect(1).to.eq(1);
  });

  it('C3 should return error when register user with used username', function () {
    console.log("it's not a test, it's a console.log");
  });
});